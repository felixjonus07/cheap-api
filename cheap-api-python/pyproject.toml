[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "cheap-api"
version = "0.1.1"
requires-python = ">=3.8"
dependencies = [
    "requests",
    "httpx"
]

[tool.maturin]
features = ["python", "pyo3/extension-module"]
module-name = "cheap_api._core"
